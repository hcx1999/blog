## 1. Camera Model
我们通过相机将3D的世界记录在2D的图像中。所以根据相机模型将成像的像素点在追溯到原发光点的过程其实就是从2D图像回溯到3D世界的过程。
首先我们考虑一个小孔成像的相机(Pinhole Camera)，他的优点是理论上每个像素点都可以追寻到原发光点$\begin{cases}x'=f\frac xz\\y'=f\frac yz\end{cases}$，缺点是小孔一旦过大就会使得多个发光点成像时重叠。
那么我们考虑为相机添加一些透镜(lens)。透镜会产生畸变，需要做相机校准(Camera Calibration)。

## 2. 深度图像与深度传感器
