## 1. 相机模型
我们通过相机将3D的世界记录在2D的图像中。所以根据相机模型将成像的像素点在追溯到原发光点的过程其实就是从2D图像回溯到3D世界的过程。
首先我们考虑一个小孔成像的相机，他的优点是理论上每个像素点都可以追寻到原发光点，缺点是小孔一旦过大就会使得多个发光点成像时重叠。
再考虑一个凸透镜成像的相机，优点是可以精准的对每个像素成像，缺点是有一定的焦距限制，焦距外的点都是模糊的。
于是我们企图把这两者结合起来，用凸透镜做相机，再用小孔成像的原理计算出来像上的每个像素点应该在什么位置。这就是相机的校准(Camera Calibration)。

## 2. 深度图像与深度传感器
